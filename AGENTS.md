# AGENTS.md

本文件适用于整个仓库，是所有智能体工作规范的唯一来源。子目录可以用更靠近内容的 `AGENTS.md` 补充局部规则；局部规则应只写差异，并保持与本文件兼容。

## 工作优先级

遇到取舍时，依次保障：

1. 学习者安全与隐私；
2. 事实、语言和答案准确；
3. 教学有效性与年龄适配；
4. 可访问性与可打印性；
5. 视觉效果与实现便利。

## 开始任务

1. 阅读 [`README.md`](./README.md) 中的项目目标、内容模型和默认学习者画像。涉及某个知识单元时，再读取该单元、其前置单元以及所在目录的局部说明。
2. 检查仓库已有模板、Skills、工具、构建配置和相似内容，优先复用现有约定。
3. 明确本次任务的知识目标、学习者画像和交付形态。用户未另行指定时，使用 README 中的默认画像；年级和教材映射只用于适配，不作为内容边界。
4. 只有当歧义会实质改变范围、正确性或安全性时才向用户确认；其余细节按本文件约定推进。

完成以上步骤的标准：能够用一句话说清“哪位学习者将通过什么活动掌握哪个可验证目标”，并知道需要检查哪些前置知识。

## 使用 Agent Skills

- 开始工作前检查当前可用 Skills；任务与某个 Skill 的用途明确匹配时，先加载并遵循它。
- Skill 负责可复用的生产或审查流程，仓库内容仍是知识与教程的来源真相。
- 同一种流程重复出现并具有稳定输入、步骤和验收标准时，可建议沉淀为项目 Skill；一次性提示词或单个知识单元不单独包装成 Skill。
- 新增或修改项目 Skill 时放入 `skills/`，并同时说明触发条件和可检查的完成标准。

## 知识单元规范

知识单元使用 `content/<subject>/<unit-slug>/` 路径。`subject` 和 `unit-slug` 使用稳定的英文 `kebab-case`；标题和学习内容使用自然、准确的中文。跨学科内容指定一个主学科，并用标签和链接关联其他学科，维护一份来源内容。

每个单元的 `unit.md` 是教学设计与共享内容的来源真相，至少包含：

- 标题、主学科和跨学科标签；
- 一个或多个可观察、可验证的学习目标；
- 前置知识单元的相对链接；没有前置项时明确写“无”；
- 难度（`foundation`、`core` 或 `stretch`）与适龄提示；
- 预计学习时间、材料和环境要求；
- 可选的年级、课标或教材映射；
- 常见误解、掌握标准与推荐下一步。

目标应描述学习者能完成的表现，例如“能用图示解释分数表示的整体与部分”，而不是“理解分数”。新增前置关系时检查链接有效，并避免循环依赖。已有知识单元可以满足需求时，扩展或链接它，不创建仅换标题的重复单元。

## 教学设计流程

每个教程默认形成“诊断—讲解—练习—反馈”闭环：

1. **诊断**：用少量低压力问题检查关键前置知识，区分不会、粗心和题意误解。
2. **讲解**：先使用具体情境或可观察现象，再连接图示、语言或符号表达；年龄适配以减少步长和提供支架实现，不牺牲概念准确性。
3. **练习**：从直接模仿到变式、应用和迁移逐层推进。题目数量服从目标，不用机械重复代替理解。
4. **反馈**：给出答案、思路、典型错误解释和可执行的下一步；错误提示先帮助定位，再逐步提供线索。

学习者达到 `unit.md` 中的掌握标准才算完成单元。诊断显示前置知识不足时，链接到相应前置单元，或在当前教程中加入明确标记的最小补救环节。

## 学习者适配

- 默认面向 README 所述的小学三年级学习者，但允许基础、核心和拓展层次并存。
- 语气尊重且具体，避免幼稚化、羞辱、竞争压力和对能力的固定标签。
- 示例优先来自儿童可理解的生活情境，同时避免性别、家庭结构、地域或文化刻板印象。
- 不在仓库中记录姓名、学校、联系方式、精确住址、成绩明细等可识别儿童身份的信息。需要记录学习偏好时使用匿名、最小化的画像。
- 用户指定教材或版本时，把它作为映射信息；生成具体对应内容前核对学科、版本和册次，不凭简称猜测。

## 学科内容要求

- **语文**：区分事实、解释与开放表达；开放题提供评价维度或示例思路，不把唯一范文当作唯一答案。
- **数学**：独立复算数值、单位、图表与答案；优先解释关系和策略，再总结规则。
- **英语**：保证表达自然、语境明确；按目标控制中文支架，并为听说任务提供可执行的练习方式。
- **物理、化学、生物**：允许适度超前，但从现象、观察和模型出发，明确日常说法与科学术语的区别。

不确定、易变化或超出常识范围的事实应查证可靠来源。引用外部资料时记录来源链接和访问日期；不复制大段受版权保护的教材或题目，使用自主改写的解释和原创练习。

## 科学活动安全

科学活动优先使用常见、低风险材料，并在教程中写明准备、成人协助、操作边界、清理和卫生要求。涉及热源、电器、尖锐物、过敏原或误食风险时，提供显眼的风险说明和更安全的替代活动。

面向儿童的居家活动采用低电压、无明火、无腐蚀、无毒、无加压、无未知微生物培养的方案。无法达到这一边界的内容改为观察、模拟、数据分析或由合格成人演示，不提供儿童独立执行的危险步骤。教程不是医疗建议；人体健康主题使用一般教育信息，并在需要时引导监护人咨询专业人士。

## 互动网页

- 先把每个互动映射到一个学习目标；预测、操作、比较、构建、即时反馈或可视化应推动理解。
- 复用仓库已有技术栈。尚无技术栈时，首个单元优先使用依赖少、可本地运行的 HTML/CSS/JavaScript 验证设计；引入框架或大型依赖前先说明长期收益。
- 使用语义化结构、清晰焦点、键盘操作、足够对比度和触控友好的尺寸；颜色不作为唯一信息通道。
- 反馈解释原因并提供分层提示，避免只显示“对/错”或过早暴露完整答案。
- 学习进度默认保留在本地；任何网络传输、账户或数据收集都需要用户明确批准并说明隐私影响。
- 改动后按仓库现有命令执行构建、类型检查和测试，并实际检查关键交互、窄屏和键盘路径。

## 可打印材料

- `printable/` 中的材料应脱离网页独立使用，并覆盖同一学习目标和学习闭环。
- 把网页互动转换为纸笔可完成的等价任务，例如绘制、排序、连线、记录、预测或讨论；保持教学功能一致，不追求界面逐像素一致。
- 默认适配 A4、黑白打印和足够的书写空间。答案与解析应便于单独打印或折叠隐藏。
- 导出前检查分页、截断、字号、图表清晰度以及题目和答案编号的一致性。

## 仓库变更原则

- 按真实需求渐进创建目录、模板和工具，不为尚未出现的内容一次性生成空结构。
- 保持改动聚焦；共享逻辑、事实和题目只维护一份来源，通过链接或构建产物复用。
- 沿用现有脚本和配置。仓库尚未定义命令时，先检查环境并记录新命令的用途，再引入最小必要配置。
- 变更内容模型或核心术语时同步更新 README；变更智能体工作方式时只更新本文件。`CLAUDE.md` 保持为本文件的轻量入口，不复制规则。
- 生成文件与构建产物分开管理；除非仓库约定需要版本化，否则提交可编辑来源，不提交可重复生成的临时文件。

## 完成标准

### 知识单元

交付前逐项确认：

- 学习目标可观察、可验证，前置关系有效且无明显循环；
- 诊断、讲解、分层练习和反馈四个阶段齐全；
- 题目、答案、单位、术语、链接和编号相互一致；
- 难度、语言和例子适合目标画像，拓展内容有清楚标记；
- 科学活动满足安全边界，材料与成人协助说明完整；
- 互动网页可运行、关键路径可操作，可打印版可独立完成同一目标；
- 已执行仓库现有的相关检查，并记录无法执行的检查及原因。

### 其他变更

文档、工具或配置任务以用户要求为准；至少验证内部链接、文件路径、示例命令和受影响的已有检查。最终回复简要列出主要改动、验证结果以及仍需用户决定的事项。

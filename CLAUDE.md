# CLAUDE.md

本仓库的智能体工作规范以 [`AGENTS.md`](./AGENTS.md) 为唯一来源。

Claude 开始任何任务前必须：

1. 完整读取并遵循根目录 `AGENTS.md`；
2. 按 `AGENTS.md` 的指针读取 [`README.md`](./README.md) 及任务相关内容；
3. 进入子目录工作时检查是否存在更具体的局部 `AGENTS.md`。

当前没有 Claude 专属例外。通用规则只修改 `AGENTS.md`，不在本文件复制，以避免两份规范失去同步。
